# Job_Break_Tracker
Break Counter is a simple web app that calculates remaining break time based on exit and entry time. It automatically subtracts used break minutes and stores the remaining time using browser localStorage, so the data persists even after page reload. Built with HTML, CSS, and vanilla JavaScript.
